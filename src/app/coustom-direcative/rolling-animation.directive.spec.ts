import { RollingAnimationDirective } from './rolling-animation.directive';

describe('RollingAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new RollingAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
